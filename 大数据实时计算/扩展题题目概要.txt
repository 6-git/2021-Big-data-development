扩展1：
改一改代码，将最近一分钟出现字符“b”的次数统计一下：
例如：最近一分钟，出现了12次

扩展2：
（1）为什么kafaka接入的数据，返回的字段buy_time并没有先后顺序
解答：返回的记录中的字段是记录中的事件发生的时间，与上传时间没有联系。在用户上传数据时，kafka并不会读取数据的具体内容，因此字段buy_time与数据的先后顺序是毫无关系的。
（2）统计乘客达到数前5的城市

扩展3：
读取Mysql的数据入kafka流

扩展4：
将Flink流计算的结果数据入到Mysql中

扩展5：
开发一个实时输入监听匹配系统。